# Add Compatibility Diagnostics from the Bootstrap Phase

Hodei Diagrams will include explicit compatibility warnings and diagnostics from the bootstrap phase of `.drawio` import/export instead of postponing that feedback until later. The decision is to make unsupported features, degraded behavior, preserved unknown data, and round-trip risks visible from the start so the Semantic Port can be evaluated honestly and improved systematically.
