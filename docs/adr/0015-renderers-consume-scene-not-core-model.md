# Renderers Consume a Scene, Not the Core Model

Hodei Diagrams render backends will consume an intermediate scene or display list generated by the Diagram Engine instead of reading the `diagram-core` model directly. The decision is to centralize semantic interpretation inside the engine so that SVG today and future accelerated backends such as WebGPU can share the same rendering meaning without duplicating product logic across renderers.
